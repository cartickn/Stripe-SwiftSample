# Uncomment this line to define a global platform for your project
# platform :ios, '6.0'

use_frameworks!

target 'Stripe-SwiftSample' do

end

target 'Stripe-SwiftSampleTests' do

end

pod 'Stripe'
pod 'PaymentKit'